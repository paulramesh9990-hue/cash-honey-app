// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  AccountType,
  AccountCreateInput,
  AccountUpdateInput,
  TransactionType,
  TransactionCreateInput,
  TransactionUpdateInput,
  BudgetType,
  BudgetCreateInput,
  BudgetUpdateInput,
  GoalType,
  GoalCreateInput,
  GoalUpdateInput,
  PortfolioType,
  PortfolioCreateInput,
  PortfolioUpdateInput,
  RecommendationType,
  RecommendationCreateInput,
  RecommendationUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// Account Client Functions
// ============================================================================

/**
 * List all Account records
 */
export async function getAccountList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<AccountType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/accounts${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Account')
  }
  const json = await response.json()
  return (json.items || []) as AccountType[]
}

/**
 * Get a single Account by ID
 */
export async function getAccountById(args: { data: { id: string; userId?: string } }): Promise<AccountType> {
  const url = `${getApiBase()}/api/accounts/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Account not found')
  }
  const json = await response.json()
  return json.data as AccountType
}

/**
 * Create a new Account
 */
export async function createAccount(args: { data: { input: AccountCreateInput; userId?: string } }): Promise<AccountType> {
  const url = `${getApiBase()}/api/accounts`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Account')
  }
  const json = await response.json()
  return json.data as AccountType
}

/**
 * Update an existing Account
 */
export async function updateAccount(args: { data: { id: string; input: AccountUpdateInput; userId?: string } }): Promise<AccountType> {
  const url = `${getApiBase()}/api/accounts/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Account')
  }
  const json = await response.json()
  return json.data as AccountType
}

/**
 * Delete a Account
 */
export async function deleteAccount(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/accounts/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Account')
  }
  return { success: true }
}

// ============================================================================
// Transaction Client Functions
// ============================================================================

/**
 * List all Transaction records
 */
export async function getTransactionList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<TransactionType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/transactions${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Transaction')
  }
  const json = await response.json()
  return (json.items || []) as TransactionType[]
}

/**
 * Get a single Transaction by ID
 */
export async function getTransactionById(args: { data: { id: string; userId?: string } }): Promise<TransactionType> {
  const url = `${getApiBase()}/api/transactions/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Transaction not found')
  }
  const json = await response.json()
  return json.data as TransactionType
}

/**
 * Create a new Transaction
 */
export async function createTransaction(args: { data: { input: TransactionCreateInput; userId?: string } }): Promise<TransactionType> {
  const url = `${getApiBase()}/api/transactions`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Transaction')
  }
  const json = await response.json()
  return json.data as TransactionType
}

/**
 * Update an existing Transaction
 */
export async function updateTransaction(args: { data: { id: string; input: TransactionUpdateInput; userId?: string } }): Promise<TransactionType> {
  const url = `${getApiBase()}/api/transactions/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Transaction')
  }
  const json = await response.json()
  return json.data as TransactionType
}

/**
 * Delete a Transaction
 */
export async function deleteTransaction(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/transactions/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Transaction')
  }
  return { success: true }
}

// ============================================================================
// Budget Client Functions
// ============================================================================

/**
 * List all Budget records
 */
export async function getBudgetList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<BudgetType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/budgets${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Budget')
  }
  const json = await response.json()
  return (json.items || []) as BudgetType[]
}

/**
 * Get a single Budget by ID
 */
export async function getBudgetById(args: { data: { id: string; userId?: string } }): Promise<BudgetType> {
  const url = `${getApiBase()}/api/budgets/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Budget not found')
  }
  const json = await response.json()
  return json.data as BudgetType
}

/**
 * Create a new Budget
 */
export async function createBudget(args: { data: { input: BudgetCreateInput; userId?: string } }): Promise<BudgetType> {
  const url = `${getApiBase()}/api/budgets`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Budget')
  }
  const json = await response.json()
  return json.data as BudgetType
}

/**
 * Update an existing Budget
 */
export async function updateBudget(args: { data: { id: string; input: BudgetUpdateInput; userId?: string } }): Promise<BudgetType> {
  const url = `${getApiBase()}/api/budgets/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Budget')
  }
  const json = await response.json()
  return json.data as BudgetType
}

/**
 * Delete a Budget
 */
export async function deleteBudget(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/budgets/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Budget')
  }
  return { success: true }
}

// ============================================================================
// Goal Client Functions
// ============================================================================

/**
 * List all Goal records
 */
export async function getGoalList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GoalType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/goals${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Goal')
  }
  const json = await response.json()
  return (json.items || []) as GoalType[]
}

/**
 * Get a single Goal by ID
 */
export async function getGoalById(args: { data: { id: string; userId?: string } }): Promise<GoalType> {
  const url = `${getApiBase()}/api/goals/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Goal not found')
  }
  const json = await response.json()
  return json.data as GoalType
}

/**
 * Create a new Goal
 */
export async function createGoal(args: { data: { input: GoalCreateInput; userId?: string } }): Promise<GoalType> {
  const url = `${getApiBase()}/api/goals`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Goal')
  }
  const json = await response.json()
  return json.data as GoalType
}

/**
 * Update an existing Goal
 */
export async function updateGoal(args: { data: { id: string; input: GoalUpdateInput; userId?: string } }): Promise<GoalType> {
  const url = `${getApiBase()}/api/goals/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Goal')
  }
  const json = await response.json()
  return json.data as GoalType
}

/**
 * Delete a Goal
 */
export async function deleteGoal(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/goals/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Goal')
  }
  return { success: true }
}

// ============================================================================
// Portfolio Client Functions
// ============================================================================

/**
 * List all Portfolio records
 */
export async function getPortfolioList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<PortfolioType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/portfolios${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Portfolio')
  }
  const json = await response.json()
  return (json.items || []) as PortfolioType[]
}

/**
 * Get a single Portfolio by ID
 */
export async function getPortfolioById(args: { data: { id: string; userId?: string } }): Promise<PortfolioType> {
  const url = `${getApiBase()}/api/portfolios/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Portfolio not found')
  }
  const json = await response.json()
  return json.data as PortfolioType
}

/**
 * Create a new Portfolio
 */
export async function createPortfolio(args: { data: { input: PortfolioCreateInput; userId?: string } }): Promise<PortfolioType> {
  const url = `${getApiBase()}/api/portfolios`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Portfolio')
  }
  const json = await response.json()
  return json.data as PortfolioType
}

/**
 * Update an existing Portfolio
 */
export async function updatePortfolio(args: { data: { id: string; input: PortfolioUpdateInput; userId?: string } }): Promise<PortfolioType> {
  const url = `${getApiBase()}/api/portfolios/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Portfolio')
  }
  const json = await response.json()
  return json.data as PortfolioType
}

/**
 * Delete a Portfolio
 */
export async function deletePortfolio(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/portfolios/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Portfolio')
  }
  return { success: true }
}

// ============================================================================
// Recommendation Client Functions
// ============================================================================

/**
 * List all Recommendation records
 */
export async function getRecommendationList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<RecommendationType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/recommendations${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Recommendation')
  }
  const json = await response.json()
  return (json.items || []) as RecommendationType[]
}

/**
 * Get a single Recommendation by ID
 */
export async function getRecommendationById(args: { data: { id: string; userId?: string } }): Promise<RecommendationType> {
  const url = `${getApiBase()}/api/recommendations/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Recommendation not found')
  }
  const json = await response.json()
  return json.data as RecommendationType
}

/**
 * Create a new Recommendation
 */
export async function createRecommendation(args: { data: { input: RecommendationCreateInput; userId?: string } }): Promise<RecommendationType> {
  const url = `${getApiBase()}/api/recommendations`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Recommendation')
  }
  const json = await response.json()
  return json.data as RecommendationType
}

/**
 * Update an existing Recommendation
 */
export async function updateRecommendation(args: { data: { id: string; input: RecommendationUpdateInput; userId?: string } }): Promise<RecommendationType> {
  const url = `${getApiBase()}/api/recommendations/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Recommendation')
  }
  const json = await response.json()
  return json.data as RecommendationType
}

/**
 * Delete a Recommendation
 */
export async function deleteRecommendation(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/recommendations/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Recommendation')
  }
  return { success: true }
}
